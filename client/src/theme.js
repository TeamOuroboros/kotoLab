import { extendTheme } from "@yamada-ui/react";

const theme = extendTheme({
  semanticTokens: {
    colors: {
      accent: "#B1CDC4",
      background: "#FFFCF7",
      foreground: "#544739",
    },
  },
});

export default theme;
