const express = require('express');
const router = express.Router();
const {} = require('../controllers/logController');

router.post('/parent', );
router.post('/childstate',);

module.exports= router;